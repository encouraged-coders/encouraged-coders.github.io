baseURL: https://encouraged-coders.github.io/
title: enCouraged coders
languageCode: en

# Add Toha using Hugo Modules
module:
  imports:
    - path: github.com/hugo-toha/toha/v4

# (Optional) allow raw HTML in Markdown if you use shortcodes/HTML
markup:
  goldmark:
    renderer:
      unsafe: true
